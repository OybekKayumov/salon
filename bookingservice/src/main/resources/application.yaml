server:
  port: 5005

spring:
  application:
    name: bookingservice
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3307/bookingdb
    username: root
    password: testtest
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  #      hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
        defaultZone: http://localhost:8070/eureka/

#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#
#spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3307/bookingdb
#spring.datasource.username=root
#spring.datasource.password=testtest
#
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver